#include "lists.h"
#include <stdlib.h>
#include<stdio.h>

/**
 * print_listin_safe - this is the function that print linked list 
 * safely, even with the loop
 *@head: this is the pointer to the head of the linked list
 *Return: this returns the number of the nodes in the list
 */

size_t print_listint_safe(const listint_t *head)
{

	listint_t *N_nodes =  NULL;
	size_t count = 0:

	while (!)
}

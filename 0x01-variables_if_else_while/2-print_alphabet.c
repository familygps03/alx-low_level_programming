#include <stdio.h>
/**
 * main - This is always main function
 * Return: It is always zero
 */

int main(void)
{
	char i;

	for (i = 'a' ; i <= 'z' ; i++)
	{
		putchar(i);
	}
	putchar("\n");
	return (0);
}

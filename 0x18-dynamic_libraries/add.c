#include <stdio.h>
#include <stdlib.h>

/**
 * add - this adds two integers
 * @a: this is the first int
 * @b: this is the second int
 * Return: it returns addition
 */

int add(int a, int b)
{
	return(a + b);
}

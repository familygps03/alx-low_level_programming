#include <stdlib.h>
#include <stdio.h>
/**
 * mul - this mul  two integers
 * @a: this is the first int
 * @b: this is the second int
 * Return: it returns mul
 */

int mul(int a, int b)
{
	return(a * b);
}
